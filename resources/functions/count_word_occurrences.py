from resources.classes.page import Page

# Assume this function are already implemented:

def count_word_occurrences(p: Page, word: str) -> int:
    """The function counts word occurrences in the given page."""
    pass